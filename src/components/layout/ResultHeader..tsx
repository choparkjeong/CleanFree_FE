import styles from "@/styles/layout/header.module.scss";

export default function ResultHeader() {
  return (
    <header className={styles["result-header-layout"]}>
      <ul>
        <li>상세페이지</li>
      </ul>
    </header>
  );
}
